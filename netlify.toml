[build]
  publish = "public"
  functions = "netlify/functions"

# Lets the frontend call clean /api/... paths instead of
# /.netlify/functions/... directly
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
